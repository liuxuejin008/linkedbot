# Copy to `.dev.vars` for `wrangler dev` (Wrangler loads secrets from that file).
# Do not commit real secrets. Production: use `wrangler secret put SECRET_KEY` etc.
#
SECRET_KEY=change-me-local-dev-use-long-random-string
JWT_SECRET=change-me-local-jwt-secret
